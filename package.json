{
  "name": "enesgelik_website",
  "private": true,
  "version": "1.0.0",
  "description": "Statik portföy sitesi — build: i18n + Cloudflare için build/ kopyası",
  "scripts": {
    "build": "node scripts/build-i18n.mjs && node scripts/copy-static-to-build.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
